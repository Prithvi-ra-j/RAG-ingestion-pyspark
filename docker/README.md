# Docker Configuration

Docker containers for deployment.

## Files

- `Dockerfile.spark` - PySpark ingestion and chunking
- `Dockerfile.embedding` - Embedding generation worker
- `Dockerfile.api` - FastAPI application
- `docker-compose.yml` - Service orchestration

## Usage

Use docker-compose to run the complete pipeline.
