# RAG Module

FastAPI-based RAG layer for retrieval and generation.

## Components

- `vector_store.py` - FAISS/Chroma vector store implementation
- `api.py` - FastAPI application and endpoints

## Usage

Start the API server to handle query requests.
